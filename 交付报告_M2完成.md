# YD 家具官网 + 后台管理系统 — 开发实施交付报告

## 一、项目概况

| 项目 | 说明 |
|---|---|
| **技术栈** | 后端 FastAPI + SQLAlchemy 2.0 + Pydantic v2 / 前台 React 19 + TS + Tailwind / 后台 React 19 + TS + AntD 5 |
| **代码组织** | monorepo：`yd-backend/` + `yd-frontend/` + `yd-admin/` + `docker/` + `shared/` |
| **仓库** | https://github.com/Iiibuw/YDjiaju（main 分支，8 个里程碑 commit） |
| **本地路径** | `D:\WorkBuddy_1\2026-08-13-09-43-40\YD家居\yd-furniture\` |
| **数据模式** | Lite 模式（SQLite + 内存验证码，零安装）；生产 docker-compose（MySQL + Redis） |

## 二、里程碑完成情况（全部 ✅）

| 里程碑 | 内容 | 端到端用例 |
|---|---|---|
| **M0** | monorepo 骨架 + Docker 编排 + 工具链（uv/pnpm 安装） | — |
| **M1** | 后端骨架 + JWT + 7 模型 + 14 端点 + 前台 MVP（3 页）+ 后台登录 | 42 |
| **M2-1** | 资讯 + 招聘（news/jobs 前后台 + 投递流程） | 84 |
| **M2-2-A** | 案例 + 部门（cases/depts 前后台 + 树形管理） | 115 |
| **M2-2-B** | 会员 + 留言（members/messages 前后台） | 143 |
| **M2-2-C** | 售后 + 招聘投递页 + 会员中心 | 143 |
| **M2-3** | 订单 + 预约 + 在线客服闭环 | **173/173** |

## 三、系统能力总览

### 后端（54 个 v1 API 端点 / 14 张表）

- **鉴权**：图形验证码 + JWT（后台 admin / 前台会员双体系）+ 登录防爆破
- **内容**：产品（三维分类/置顶/状态机）、资讯（发布/置顶/推荐/浏览量）、案例（风格/面积/富文本）、招聘（社招/校招/投递 5 阶段）
- **组织**：部门树（path 编码 + 子部门删除保护）
- **交易**：订单（7 状态流转 + 金额快照 + 产品快照）、预约（4 类型 + 跟进状态）、留言（回复闭环）
- **数据权限**：DataScope 白名单基座 + 软删除全表覆盖

### 前台（13 页面 + 4 组件）

首页 / 产品中心 / 产品详情（下单+预约）/ 资讯×2 / 案例×2 / 招聘×2（投递表单）/ 关于我们 / 联系我们（留言表单）/ 售后服务 / 会员中心（我的订单+预约）

全局组件：**在线客服悬浮窗**（关键词机器人）+ **预约到店 Modal** + 立即购买 Modal

### 后台（9 个管理模块）

资讯 / 招聘（含投递记录）/ 案例 / 部门（树）/ 会员 / 留言 / 订单（状态流转）/ 预约（跟进）/ 登录

## 四、验证结果

- **端到端测试**：`scripts/smoke-test-lite.py` — **173 个用例全部通过**（含鉴权、CRUD、状态机、软删除、数据隔离）
- **类型检查**：前后台 strict 模式 0 错误
- **生产构建**：前台 259KB（gzip 78KB）/ 后台 1.31MB（gzip 416KB）

## 五、启动方式

```bash
# Lite 模式（零安装）
cd yd-backend && export UV_LINK_MODE=copy
uv run python scripts/init_lite.py          # 建表 + 种子
uv run uvicorn app.main:app --port 8000     # 后端

cd yd-frontend && pnpm dev                  # 前台 :5180
cd yd-admin && pnpm dev                     # 后台 :5181
```

**演示账号**：后台 `admin/admin123`；会员 `13800138001/member123`

## 六、后续建议

1. **M3 部署阶段**：Docker Desktop 安装后 `docker compose up` 一键拉起全套（MySQL/Redis/Nginx 生产化）
2. **安全加固**：Redis 替换内存验证码、CORS 收紧、限流中间件
3. **功能扩展**：购物车、SKU、支付对接、数据大屏
